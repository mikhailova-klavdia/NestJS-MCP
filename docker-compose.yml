services:
  web:
    build: .
    ports:
      - "8000:5000"
  redis:
    image: "redis:alpine" 
  
  postgres:
    image: ankane/pgvector:latest
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: root
      POSTGRES_DB: test
    ports:
      - "5432:5432"